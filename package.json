{
  "name": "ultimate_junie_config",
  "version": "1.0.0",
  "scripts": {
    "build": "bun src/build-agents.ts",
    "local": "bun -e \"const pkg = await Bun.file('package.local.json').json(); const { execSync } = require('child_process'); execSync(pkg.scripts[process.argv[1]], { stdio: 'inherit' });\""
  }
}
